package com.mosaic.client.ui.screens.expert;

import javafx.fxml.FXML;

/**
 * Controller for the Expert Selection screen.
 *
 * TODO APP-4 (#8) — Full implementation
 *   AC1: Add filters panel (domain / source / availability dropdowns — no logic required yet)
 *   AC2: Populate a ListView with hardcoded expert entries
 *   AC3: Selecting an expert updates the detail/preview panel on the right
 *   AC4: "Confirm" button → Navigator.showWorkspace()
 *         (pass selected expert name as a placeholder; real wiring comes later)
 *   AC5: "Download" button → show a placeholder Alert ("Download not yet available")
 */
public class ExpertSelectionController {

    @FXML
    public void initialize() {
        // TODO APP-4 (#8): Populate hardcoded expert list into ListView
    }
}
